### Merge the DPO adapter onto the SFT model -> a standalone fp16 model for eval.
model_name_or_path: models/sft-merged
adapter_name_or_path: outputs/dpo
template: mistral
finetuning_type: lora
export_dir: models/dpo-merged
export_size: 5
export_device: cpu
export_legacy_format: false
